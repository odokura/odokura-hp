---
title: "Takaneko 検証計画"
sidebar_label: 検証計画
draft: true
sidebar_position: 11
---

# 検証計画

## 方針

Takaneko は作者本人だけが localhost で使う private アプリケーションである。一次情報の収集、保存、
正規化、企業ファクト、ローカル表示候補を再現可能な固定データで検証する。X API・外部通知・第三者への
再配信は実装も検証も行わない。

実在する情報源への接続試験は、その情報源の利用条件・契約・書面許可を確認した後だけに行う。

## 固定データ

有効な種別ごとに、正常文書、必須項目欠落、曖昧な要素、数値不整合、訂正文書、重複文書を網羅する。
フィクスチャは許可された情報源から採取し、原本を含むため Git 管理外の `fixtures/` に置く。期待値は
作者本人が原本と突合して確定する。

## 単体テスト

| 対象 | 確認すること |
| --- | --- |
| 文書判定 | 表題パターン、メタデータ抽出、XBRL 添付有無 |
| 文書正規化 | 見出し階層、段落位置、XBRL 要素・コンテキスト・単位を source_excerpt として再特定できる |
| structured-parser | 要素候補の一意解決、あいまいな要素・単位・期間の拒否 |
| 財務比較 | 実績値の抽出、前年同期比較、会計期間・単位・連結範囲の取り違え防止 |
| ローカル表示 | 固定テンプレートが企業名・コード・分類・時点・根拠を含むこと |
| 重複 | 同一文書・根拠箇所・主題の再処理で company_fact を重複作成しないこと |
| 設定 | `mode: "live"` と無効な ContentSpec・設定 JSON を拒否すること |

## 結合テスト

一時 SQLite DB と固定の一次情報文書を使い、保存、正規化、根拠箇所、ContentSpec 選択、企業ファクト、
ローカル表示候補までを通して確認する。

1. 表形式の決算文書から、検証可能な一つの数値比較を含む company_fact を作る。
2. 公式タイトルだけの文書・XBRL なしの文書は保存するが、表示候補を作らない。
3. 同じ企業ファクトの再処理が重複レコードを作らない。
4. 訂正文書が原記録を上書きせず、訂正関係を保存する。
5. 保存失敗、キルスイッチ、根拠不足が新しい表示候補を抑止する。
6. DB 再起動後も収集位置・設定版・企業ファクトを保持する。

## リプレイ検証

フィクスチャ一式を SQLite に投入し、ContentSpec と対象銘柄集合の指定版で再生する CLI を用意する。
文書数、種別別の company_fact 数、ローカル表示候補数、抑止理由別・銘柄別の件数を出力し、同じ入力と
設定版から同じ結果になることを確認する。

## 運用検証

- SQLite マイグレーション途中の失敗で bot が起動しない。
- 保存失敗と DB 整合性エラーで新規分析・ローカル表示が停止する。
- 正常サイクルの途絶で watchdog が全体キルスイッチを入れる。
- lab の読み取り専用接続を開いたまま bot の保存と WAL チェックポイントが継続する。
- bot / lab が localhost 外へ待受・投稿・通知を行わない。
- GitHub に DB、収集データ、フィクスチャ原本、認証情報が含まれない。

## 実装完了の基準

TypeScript の型検査、種別別の単体テスト、結合テスト、固定データによるリプレイ検証、Docusaurus ビルドが
成功し、生成されるローカル表示がコンテンツ仕様を満たすことを確認する。さらに、許可された実データを
対象に 14 日間以上 private 運転し、数値・単位・会計期間・根拠の誤りがないことを作者本人が確認する。
